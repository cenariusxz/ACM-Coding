这个题就是在n长的数列中取第k小，n较大，但是数值范围较小，希望考察计数而不是直接sort。
很不幸的是，OJ当时无法上传较大的数据文本，所以n没有办法设置的太大，导致有C++使用sort在900+ms卡过的。加上没有签到题榜不太好看，时限中途被改成了2s，最后成了python都能直接sort过的水题，非常无奈。
其实重点在于数值范围只有1到10000，那么读入的数都不需要存进数组，直接记录每一个数值出现了多少次就可以了，然后取第k小时只需要对次数数组从小往大遍历一遍，维护小于等于当前数值的数的个数，当个数第一次大于等于k时，当前值就是第k小的数的值了，复杂度是读入的O(n)+遍历的O(10000)。
